import { Button } from "antd";

const HomePage = () => {
    return (
        <div className="min-h-screen p-8">
            <h1 className="text-3xl font-bold mb-4">Chào mừng đến với Shop Áo</h1>
            <Button type="primary">Xem sản phẩm</Button>
        </div>
    );
};

export default HomePage;
